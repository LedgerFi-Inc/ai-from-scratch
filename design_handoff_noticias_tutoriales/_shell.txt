Notas de trabajo. No es un artboard: el helper solo mira los .dc.html.

TOKENS Y CLASES, copiados de la fuente y sin redondear
  web/src/lib/theme-css.ts  y  web/src/layouts/App.astro

  --f  -apple-system,'SF Pro Display','SF Pro Text','Helvetica Neue',system-ui,sans-serif
  --m  ui-monospace,'SF Mono',SFMono-Regular,Menlo,monospace

  OSCURO  --bg #000  --panel #0B0B0C  --l1 #fff  --l2 rgba(235,235,245,.62)
          --l3 rgba(235,235,245,.50)  --hair rgba(84,84,88,.46)
          --hair2 rgba(84,84,88,.16)  --fill rgba(120,120,128,.22)
          --ac #0A84FF  --ac-solid #0A6CFF  --ok #30D158  --or #FF9F0A --rd #FF453A
          --btn-bg #fff --btn-fg #000 --on-ac #fff
  PAPEL   --bg #F2F2F2 --panel #fff --l1 #000 --l2 rgba(0,0,0,.66)
          --l3 rgba(0,0,0,.58) --hair rgba(0,0,0,.22) --hair2 rgba(0,0,0,.08)
          --fill rgba(120,120,128,.20) --ac #0A5AD6 --ok #0C6B3E --or #8A5000
          --rd #C21B12 --btn-bg #000 --btn-fg #fff

  .h1   700 34px/1.06  -.035em        (App.astro:82)
  .h2   700 21px/1.16  -.028em        (App.astro:83)
  .h3   600 15px/1.3   -.01em
  .p    400 16px/1.5   --l2
  .s    400 13px/1.45  --l3
  .lbl  500 10px/1 mono .18em MAYUS --l3
  .eb   600 10px/1 mono .22em MAYUS --ac
  .card 1px --hair2, padding 22
  .btn  h44 radio 6 bg --btn-bg fg --btn-fg, 600 11px mono .1em MAYUS
  .chip min-h 46 pad 0 14 borde --hair, 500 12px mono; [data-on] borde --ac + bg --fill
  .input h44 pad 0 14 bg --fill borde --hair2, 400 15px
  .seg  borde 1px --hair2 ; .segb h30 pad 0 10, 600 10px mono .12em MAYUS
        [aria-pressed] bg --ac-solid color #fff
  .nav  h44 gap 11 pad 0 16 --l3, 500 11px mono .14em MAYUS, borde-izq 2px transparente
        [aria-current] --l1 + bg --fill + borde-izq --ac
  .sidebar 248px, borde-der 1px --hair2 ; .sidebar-head h64
  .app-head h64 pad 0 28, borde-inf 1px --hair2
  ESQUINAS CUADRADAS en todo salvo .btn (6px).

ICONOS  web/src/lib/icons.ts — rejilla 24, trazo 1.6, caps y joins redondos.
  Se reutilizan home/book/chart/user/gear/spark/clock/arrow/lock/menu.
  Faltaban dos y se dibujan en la MISMA gramatica, para añadir a icons.ts:
    news  'M4.2 6.2A1.4 1.4 0 0 1 5.6 4.8h9.6a1.4 1.4 0 0 1 1.4 1.4v11.2a2 2 0 0 0 2 2H6.2a2 2 0 0 1-2-2zM16.6 9.4h1.8a1.4 1.4 0 0 1 1.4 1.4v6.6a2 2 0 0 1-2 2M7.2 8.2h6.4M7.2 11.6h6.4M7.2 15h4'
    guide 'M4.6 6.4h2.4M4.6 12h2.4M4.6 17.6h2.4M10.6 6.4h8.8M10.6 12h8.8M10.6 17.6h5.8'

COPY  Es un borrador en la voz del producto (CLAUDE.md, "Content voice"):
  frases cortas, sin jerga sin traducir, y NINGUNA cifra inventada. Por eso las
  noticias son TENDENCIAS y no partes con fecha y fuente: inventar un titular
  atribuido seria inventar un hecho. Reemplazar por lo que se publique de verdad.

VERIFICADO EN NAVEGADOR (30-ago-2026), no deducido
  Playwright entra al iframe del artboard; el CLI de /browse no (origen opaco).
  - render: los 4 artboards montan. Noticias mostraba 4 de 5 tendencias con el
    marco fijo a 900/844 + overflow:hidden. Arreglado subiendo el alto al de la
    PAGINA (1440x1100 y 390x1180): App.astro no tiene ni un panel con scroll
    propio (grep overflow-y|scrollbar = 0), scrollea el documento.
  - filtro: click en el chip "Riesgos" -> chips [Riesgos=ACTIVO], eyebrows
    ["Riesgos"], n=1. De 5 a 1. Los onClick="{{t.pick}}" quedan cableados
    (typeof onclick === 'function' en los 5 chips).
  - iconos: 9 de 9 paths resueltos, 0 sin resolver.

RUIDO CONOCIDO, sin efecto
  La consola escupe por artboard:
    <path> attribute d: Expected moveto path command ('M' or 'm'), "{{n.d}}"
  Es el parseo del template ANTES de hidratar: el validador de SVG mira el
  atributo d crudo. Se queda: los 9 iconos resuelven despues (medido arriba) y
  esquivarlo obligaria a duplicar los 9 paths literales fuera del sc-for, que
  es peor que el ruido. Un hueco en style no da error porque el CSS invalido se
  descarta en silencio; d no.
